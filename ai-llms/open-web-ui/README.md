# Open WebUI

Interface principal para consumir o `Ollama`.

## Uso

```bash
cp .env.example .env
docker compose up -d
```

## Notas

- Usa `OLLAMA_BASE_URL=http://ollama:11434` por padrão.
- Entra na rede compartilhada `ai-llms-network`.
